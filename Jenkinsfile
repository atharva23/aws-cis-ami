pipeline {
    agent any
    
    stages {
        
        
        stage('Packer Build ') {
            steps {
                
                sh 'sudo ./buildami.sh'
               
                
               
            }
        }
    }
}
